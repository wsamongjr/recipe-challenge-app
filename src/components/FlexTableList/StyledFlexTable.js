import styled from "styled-components";

const StyledFlexTable = styled.div`
    display: flex;
    flex-flow: column nowrap;
    font-size: 16px;

    .table-row {
        display: flex;
    }
`;

export default StyledFlexTable;
